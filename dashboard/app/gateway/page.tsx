import { GatewayLab } from '@/components/gateway-lab'

export const metadata = {
  title: 'Gateway — SYNAPSE',
  description: 'x402 payment-required gateway protocol lab.',
}

export default function GatewayPage() {
  return <GatewayLab />
}
